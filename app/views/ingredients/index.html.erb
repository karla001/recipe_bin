<h1>Ingredients</h1>

<ul>
    <% @ingredients.each do |ingredient| %>
        <li>
            <strong> <%= ingredient.name %> </strong>  <%= ingredient.portion %><%= ingredient.p_unit_of_measure %>
        </li>
    <% end %>
</ul>
<%=link_to "Add Ingredients", new_user_recipe_ingredient_path(current_user.id, @recipe)  %>
